# 科研论文助手 Agent 面试问题

版本：v1.6 · 2026-09-22 · 状态：后续教学题库，尚未开始提问或学习验收。

本题库服务于 AI 应用 / Agent 工程师岗位。应用场景已确定为“科研论文助手 Agent”：使用 QASPER 建立单篇论文证据问答基准，逐步支持自选论文、多轮阅读、可信生成和反馈驱动的策略改进。目标是能解释设计、读懂代码、复现实验、处理失败，而不是背诵原项目的经历和成果数字。

## 使用方式与验收边界

- **现在只保存题库，不开始提问。** 每阶段工程完成后，结合实际实现抽取 3–5 道题；用户准备好时再进行学习验收。不会一次问完 56 题。
- 每题先问核心问题，再根据回答选择递进追问；允许打开代码、画数据流、运行实验。逐渐过渡到脱离代码讲解，避免把记忆力当成工程能力。
- **工程验收**依据可运行代码、测试、评测报告和失败处理；**学习验收**依据用户的解释与操作。自动化测试通过不代表用户已掌握，题库建好也不代表面试已通过。
- 题目涉及未来模块时按“设计讨论”处理；只有实际实现并验证后才能标记“实现验收通过”。未做的多租户、K8s、PDF 图表解析等必须说明范围，不能靠口头回答算完成。
- 原项目介绍和源码作为技术参考。学校落地、团队分工、数据规模、提升比例和吞吐数字都不自动成为新项目事实。面试回答中的数字必须指向新项目自己的报告与运行条件。
- 每题记录：日期、阶段、当前提交、证据路径、回答要点、缺口、复测结果。建议状态为“未提问 / 需补原理 / 需补实验 / 已掌握”；初始全部为“未提问”。

## 八个提问维度

| 维度 | 编号 | 面试官重点考察 |
|---|---|---|
| 业务与架构选型 | A01–A07 | 为什么做、边界在哪里、为何选这套前后端与基础设施、何时换方案 |
| 数据与评测 | B01–B08 | 数据是否可用、证据如何对齐、是否泄漏、指标与补充元数据究竟证明了什么 |
| 检索与可信证据 | C01–C07 | BM25、向量、融合、重排、回源、引用核验与拒答 |
| Harness 与工具治理 | D01–D07 | 谁控制执行、如何限制模型、如何处理失败与留下可回放记录 |
| 记忆与多轮对话 | E01–E07 | 指代、切换论文、上下文预算、权威边界与记忆生命周期 |
| 反馈与可执行 Skill | F01–F07 | 反馈如何改变行为、如何归因、如何评估、灰度、回滚 |
| 存储与权限 | G01–G07 | 事实与索引的边界、上传入库、版本一致性、对象访问与多租户隔离 |
| 可靠性、性能与工程复盘 | H01–H07 | 异步任务、故障、指标、成本、负载与扩容是否有真实证据 |

## 阶段对应关系

| 阶段 | 本阶段范围 | 建议首次抽题；复习时另选 |
|---|---|---|
| P0 规划与取舍 | 场景、数据方案、架构决策、学习与验收规则 | A01、A02、A04、B01 |
| P1 数据审计与证据基线 | QASPER train/dev 审计、单篇论文 BM25 证据检索、CLI 和最小 API；不调用生成模型、不做前端 | B02、B03、C01、C02、H01 |
| P2A 持久化工作台 | 数据库、对象存储、事实回查、前后端和可调整阅读布局 | A03、A04、G01、G03 |
| P2B 可信混合检索与生成 | 向量、融合、重排、结构化引用、证据校验与拒答；按模块逐步加入 | C03、C04、C06、C07 |
| P3 Harness 与 API | 显式执行状态、接口契约、工具边界、Trace、预算与故障处理 | D01、D02、D03、D06 |
| P4 前端与交互 | 论文、证据、状态、反馈入口以及错误恢复；上传能力以完成的入库管线为前提 | A03、D07、G03、G04 |
| P5 多轮与分层记忆 | 论文范围、指代、摘要、偏好、来源和删除策略 | E01、E02、E04、E06 |
| P6 反馈与策略改进 | bad case、候选 Skill、回放、发布与回滚 | F01、F03、F04、F06、F07 |
| P7 综合评测与复盘 | 冻结配置、分层评测、消融、失败复盘与演示 | B05、B06、B07、H04、H07 |
| P8 可选工程扩展 | 团队空间、高并发、服务拆分、队列与扩容等按需要实施 | G05、G06、G07、H02、H06 |

以下阶段标签表示何时讨论或复测，不表示相关模块已经实现。规划架构为 React + Vite、FastAPI、PostgreSQL + pgvector、S3 兼容对象存储；Redis 按会话、缓存和任务需求分阶段加入。P1 的本地基线不能被描述成已经部署了整套架构。

## A. 业务与架构选型

### A01｜这个 Agent 具体帮谁完成什么任务？【P0、P7】

- 核心问题：用一个真实阅读任务说明用户输入、系统输出、可信要求与产品边界。
- 递进追问：单篇论文问答、全库论文发现、跨论文比较各有什么不同？什么时候只返回证据或要求澄清？
- 需要出示：一条完整用户流程、范围说明和当前可运行演示。
- 验收要点：明确当前任务以指定论文为范围；不把待做功能当成现有能力，不承诺结论绝对正确。

### A02｜为什么从行政助手改成科研论文助手？【P0、P7】

- 核心问题：QASPER 为什么适合本项目，哪些能力能被它直接评测，哪些不能？
- 递进追问：与 MultiDoc2Dial 相比获得和失去什么？没有多轮标注，是否意味着不能做多轮？
- 需要出示：数据选择记录、补充场景测试清单及一个论文问答样例。
- 验收要点：区分数据集提供的标注与系统需实现的工程能力；能说明选择的代价。

### A03｜为什么选 React + Vite，什么时候会选其他前端？【P0、P4】

- 核心问题：论文阅读、证据跳转、任务状态与流式回答对前端提出哪些要求？
- 递进追问：为什么当前不优先 Next.js、Vue、Streamlit 或 Gradio？这些选择在哪些条件下更合理？左右分栏为什么自己实现受控拖动，怎样兼顾指针、键盘、窄屏和状态持久化？
- 需要出示：前端决策记录、页面状态设计，以及加载、取消、失败、重试和分栏调整演示。
- 验收要点：从产品需求、部署方式、SEO、交互复杂度解释；不以“流行”或个人熟悉程度作为唯一理由。

### A04｜为什么选 FastAPI，Python 的代价是什么？【P0、P3】

- 核心问题：检索、评测、模型 SDK 与 API 服务为什么适合放在 Python 后端？
- 递进追问：async 能解决 CPU 密集计算吗？何时改用进程或 Worker？Node.js、Django、Spring Boot 有哪些优势？
- 需要出示：请求处理路径、阻塞操作清单、接口 Schema 与依赖边界。
- 验收要点：分清异步 I/O、线程、进程、并行计算；不把 async 写法等同于高吞吐保证。

### A05｜为什么用 PostgreSQL + pgvector，原来的 MongoDB + Milvus 呢？【P0、P2、P8】

- 核心问题：当前规模、关系约束、事务、运维成本与检索要求如何影响选型？
- 递进追问：同一个数据库能否仍区分事实和检索候选？数据规模或延迟到什么程度需要重新评估 Milvus？
- 需要出示：数据模型、索引方案、当前规模与一项可重复检索实验。
- 验收要点：解释逻辑职责与物理部署的区别；承认两种架构的适用范围，不声称 pgvector 在所有规模都更优。

### A06｜为什么先做模块化服务，而不直接拆多个 Agent 服务？【P0、P3、P8】

- 核心问题：进程、服务、Agent、DAG 节点分别是什么？五个节点是否等于五个服务？
- 递进追问：论文领域分类能否直接成为服务边界？何种资源、团队或隔离需求才值得拆分？
- 需要出示：模块依赖图、部署图和一个明确的后续拆分条件。
- 验收要点：能给出拆分收益与网络、观测、数据一致性成本；不为复刻原简历强行多服务化。

### A07｜为什么自建 Harness，和 pi、LangGraph 或应用平台是什么关系？【P0、P3、P7】

- 核心问题：框架提供什么，自建层必须承担什么？本项目是否真的引入了 pi Runtime？
- 递进追问：框架也能做状态、重试和工具限制，为什么仍选择当前实现？何时值得改用框架？
- 需要出示：运行时适配接口、架构决策、复用与自写模块清单。
- 验收要点：依据实际代码讨论，不能声称框架天然无法治理；未使用 pi 时不能写进已实现技术栈。

## B. 数据与评测

### B01｜你怎样判断一个公开数据集“现成可用”？【P0、P1】

- 核心问题：下载成功之外，还要检查哪些内容？
- 递进追问：出处、许可、数据版本、Schema、缺失值、分割和证据覆盖如何记录？结构化正文能证明 PDF 解析能力吗？
- 需要出示：来源链接、获取时间、文件哈希、数据清单和审计报告。
- 验收要点：能复现同一份数据；明确数据集许可与用户另行上传论文的使用条件不能混为一谈。

### B02｜QASPER 的论文、问题、答案和证据怎样对应？【P1】

- 核心问题：从原始 JSON 走到可检索段落，再走到评测标签，ID 如何保持对应？
- 递进追问：一道题多个标注答案、多个证据、重复段落、空证据、无法对齐的文字分别怎么处理？
- 需要出示：一条原始记录、转换结果、稳定 ID 规则、对齐率及失败样本。
- 验收要点：不默默丢弃异常；能解释段落粒度与原始证据粒度不一致时的匹配口径。

### B03｜哪些数据可以进检索库，哪些只能用于评测？【P1、P6、P7】

- 核心问题：论文正文、标准答案、问题和证据标签分别放在哪里？
- 递进追问：测试论文正文能作为检索语料吗？为什么不能把标准答案当知识片段？反馈调参能否使用冻结验收题？
- 需要出示：入库字段白名单、标签隔离代码和数据分割配置。
- 验收要点：区分“允许读取待问论文正文”和“使用答案标签作弊”；训练/开发用于迭代，验收标签不可反复调优。

### B04｜补充多轮、权限和版本测试怎样保证质量？【P1、P5、P8】

- 核心问题：哪些测试来自 QASPER，哪些需要人工设计或合成，如何分别标记？
- 递进追问：模型出题后再自己判分有什么风险？单篇论文问答结果能否代表跨论文能力？
- 需要出示：样例来源字段、人工核对规则、困难样本与独立的工程场景测试。
- 验收要点：不把合成题说成原生基准或真实用户反馈；新增任务有独立范围与验收标准。

### B05｜Recall@k、命中率、MRR 和证据 F1 分别衡量什么？【P1、P7】

- 核心问题：一个问题有三个相关段落，只找回一个，Recall@k 和“至少命中一个”的数值一样吗？
- 递进追问：没有可对齐文本证据的题进入哪个分母？多个答案标注如何汇总？只算成功映射的题会带来什么偏差？
- 需要出示：手算小例子、指标实现、分母与排除原因统计。
- 验收要点：准确区分各指标；覆盖率与性能一起报告，不把检索指标叫作答案准确率。

### B06｜怎样证明混合检索和反馈策略真的带来提升？【P2、P6、P7】

- 核心问题：如何设置基线、对照、消融与固定评测条件？
- 递进追问：同时换模型、top-k 和提示词，能把全部提升归给 Skill 吗？样本少、随机波动或问题难度变化怎样处理？
- 需要出示：同题运行配置、逐项对比、失败分层与不确定性说明。
- 验收要点：控制变量，区分百分点与相对提升；不因几条演示成功就宣称总体有效。

### B07｜如何评估答案、拒答和图表问题？【P2、P7】

- 核心问题：引用存在、引用支持结论、答案完整与拒答正确是否是同一件事？
- 递进追问：检索没找到就能认定论文没写吗？图表题暂不支持时如何报告？同一模型生成与裁判有什么风险？
- 需要出示：评审 rubric、可答/不可答分层、图表子集统计和人工核验样本。
- 验收要点：同时关注错误作答和过度拒答；明确是正文范围内缺证据，不推断作者一定未做实验。

### B08｜论文时间、方向、PDF 和 CCF 数据从哪里来，能证明什么？【P2A、P7】

- 核心问题：为什么不能直接从 QASPER 或 arXiv 编号声称正式发表时间和 CCF 级别？
- 递进追问：arXiv `<published>`、`primary_category`、交叉分类、PDF link 和 `journal_ref` 各是什么；为什么不从标题推断方向；会议只认可 full/regular paper 时如何避免把 workshop 误标；为什么筛选与排序要在分页前由后端完成？
- 需要出示：冻结元数据快照、独立数据库表、方向映射、保守 CCF 规则、外部 PDF 与本地结构化正文的差别、来源链接和 API 测试。
- 验收要点：主分类聚合便于浏览，但不等于唯一研究领域；CCF 是发表载体目录级别，不是论文质量评分；外部 PDF 链接不冒充已存储和哈希校验的本地对象；区分补充元数据、RAG 正文与评测 gold。

## C. 检索与可信证据

### C01｜为什么 P1 先做 BM25？【P1、P2】

- 核心问题：BM25 如何利用词频、逆文档频率和长度归一化？
- 递进追问：模型名、数据集缩写、数字和同义表达分别容易怎样出错？它与 TF-IDF、向量检索各有何取舍？
- 需要出示：分词结果、打分代码和一组成功/失败检索。
- 验收要点：能解释参数与排序变化；把基线定位为可测的起点，不把无模型检索包装为生成 Agent 已完成。

### C02｜论文怎样切块，切得越小越好吗？【P1、P2】

- 核心问题：为什么需要保留论文、章节、段落和版本信息？
- 递进追问：定义在上段、结论在下段怎么办？overlap 会造成哪些重复？单论文候选范围如何保证不混入其他论文？
- 需要出示：切块样例、证据到 chunk 的映射和不同粒度的检索比较。
- 验收要点：平衡召回、完整语义、成本与引用定位；保留可回源关系。

### C03｜向量检索到底检索了什么？【P2、P8】

- 核心问题：embedding、相似度与近似最近邻之间的关系是什么？
- 递进追问：距离类型和归一化如何匹配？换 embedding 模型为什么通常要重建索引？ANN 加权限过滤会发生什么？
- 需要出示：模型与索引版本、精确/近似检索对比和过滤后的召回检查。
- 验收要点：不把相似度解释为事实正确概率；能说明 HNSW 等索引的召回、内存与时延取舍。

### C04｜为什么要融合、再重排？【P2】

- 核心问题：BM25 和向量分数不能直接相加时，RRF 怎样工作，Reranker 又解决什么？
- 递进追问：RRF 常数、候选规模、去重方式如何影响结果？重排会不会找回候选里根本没有的证据？
- 需要出示：手算一组排名融合、各检索阶段的候选记录和消融结果。
- 验收要点：说明排名融合与相关性重排的区别；准确说出重排无法弥补候选全集缺失。

### C05｜Query Rewrite 如何提高召回，又如何避免改错问题？【P2、P5】

- 核心问题：缩写展开、多查询和指代补全分别依赖什么输入？
- 递进追问：能否把用户问“基线方法”改成“本文方法”？论文范围和数字约束如何保留？改写失败如何退回？
- 需要出示：原问题、改写结果、来源实体和前后检索对照。
- 验收要点：改写受范围与原意约束；保留原始查询，限制查询数量和成本，并覆盖语义漂移测试。

### C06｜为什么检索候选还要回源核验？【P2、P8】

- 核心问题：向量返回的文本为什么不能直接视为可信证据？
- 递进追问：论文更新、权限撤销、chunk 删除后旧索引还在怎么办？回源后证据不足是否允许补回已过滤内容？
- 需要出示：候选 ID、事实读取、版本/权限检查与陈旧索引故障用例。
- 验收要点：以当前可读事实为准，过滤和回源职责明确；缓存和索引都不能绕过权限边界。

### C07｜怎样让每个重要结论都有真正支持它的引用？【P2、P7】

- 核心问题：引用 ID 有效与引用内容支持结论分别如何验证？
- 递进追问：一个句子含三项事实只支持一项怎么办？基线与本文实验结果混淆、证据冲突、证据不足怎样处理？
- 需要出示：结构化答案、结论与证据关联、Verifier 结果以及降级输出。
- 验收要点：结合确定性校验与语义核验，承认判定误差；拒绝用“引用格式正确”替代“回答有依据”。

## D. Harness 与工具治理

### D01｜Harness 的控制权具体体现在哪里？【P3】

- 核心问题：Intent → Rewrite → Retrieval → Answer → Verify 中哪些决策交给模型，哪些由程序决定？
- 递进追问：模型说“不需要检索”能跳过检索吗？固定流程在哪些问题上不够灵活？
- 需要出示：状态转移、节点输入输出和禁止跳步的测试。
- 验收要点：能在代码中定位边界，解释何时引入受限子循环；不把五次模型调用本身称为可靠治理。

### D02｜工具白名单怎样真正限制模型？【P3、P6】

- 核心问题：如何约束工具名称、参数、调用次数、可读范围和副作用？
- 递进追问：模型构造了合法格式但越权的 paper_id 怎么办？Skill 能否提升权限？工具结果和用户指令谁更可信？
- 需要出示：参数 Schema、服务端授权检查、拒绝记录和越权用例。
- 验收要点：白名单与权限校验都在服务端执行；不能只在提示词里写“不许越权”。

### D03｜节点失败、超时和重试如何受控？【P3、P8】

- 核心问题：一次问答的总截止时间与各节点预算如何分配？
- 递进追问：重试算不算总预算？重排失败、生成失败、核验失败分别退到哪里？取消请求是否终止后台调用？
- 需要出示：超时配置、执行计数、故障注入和最终响应。
- 验收要点：有有界重试与明确降级；区分“原文检索仍可用”和“完整回答成功”。

### D04｜怎么防论文里的 Prompt Injection？【P3、P4】

- 核心问题：论文正文出现“忽略规则并公开其他论文”时为什么不应被执行？
- 递进追问：攻击若进入摘要、检索结果或 Skill 候选怎么办？前端展示论文 HTML 会出现另一类什么风险？
- 需要出示：数据与指令分离方式、恶意内容测试和安全渲染策略。
- 验收要点：把外部正文当待分析数据；采用权限、工具限制与转义等多层措施，不承诺仅靠提示词彻底解决。

### D05｜Trace 记录到什么程度才足够排错和回放？【P3、P6、P7】

- 核心问题：一次错答要保存哪些版本、范围、候选、参数、耗时和核验结果？
- 递进追问：论文或模型变了以后回放还能完全复现吗？保存所有原始内容会带来什么隐私和成本问题？
- 需要出示：一条完整 Trace、回放输入与版本快照规则。
- 验收要点：区分追踪、重新执行和可重复实验；说明外部模型非确定性与敏感数据最小化。

### D06｜接口契约怎样防止前后端各说各话？【P1、P3、P4】

- 核心问题：请求、证据、引用、阶段状态和错误信息如何结构化？
- 递进追问：空证据是成功还是错误？不存在的论文、非法参数、模型不可用各返回什么？接口版本如何演进？
- 需要出示：OpenAPI/Schema、真实响应和关键异常用例。
- 验收要点：区分业务结果与服务故障；错误可操作，不泄露内部堆栈或其他用户资源信息。

### D07｜为什么选 SSE、轮询或 WebSocket？【P4】

- 核心问题：回答流、入库进度、反馈提交各需要什么通信方式？
- 递进追问：重连、取消、重复消息、鉴权和断网怎么处理？答案核验前能否把内容展示为“可信结论”？
- 需要出示：事件协议、浏览器网络记录和断流恢复演示。
- 验收要点：按通信方向与复杂度取舍；明确草稿与已核验状态，不能流完后才悄悄把错误答案当作成功。

## E. 记忆与多轮对话

### E01｜“它用了什么数据集”中的“它”是谁？【P5】

- 核心问题：当前论文、讨论对象和历史消息如何帮助确定指代？
- 递进追问：先问论文 A，再切到 B，随后说“它”怎么办？同时出现两个方法时，何时应要求澄清？
- 需要出示：状态变化、查询改写和包含歧义的连续对话测试。
- 验收要点：不能只依赖盲目追加历史；明确已确认实体、切换规则和澄清状态。

### E02｜为什么要分层记忆，不直接保存完整聊天记录？【P5】

- 核心问题：工作记忆、情景记忆、用户偏好、共享知识、程序性策略分别起什么作用？
- 递进追问：哪些层当前确有必要？一定要建五个库吗？论文事实与这些记忆是什么关系？
- 需要出示：实际存储映射、读取入口和一条上下文构建结果。
- 验收要点：按职责划分，允许渐进实现；不把“五层”当形式要求，不让记忆自动取得论文证据资格。

### E03｜MemoryContextBuilder 怎样决定保留什么？【P5】

- 核心问题：权限、论文范围、时效、来源可信度和 token 预算按什么顺序生效？
- 递进追问：字符数是否等于 token 数？中文问题、英文论文混合时怎样预算？被裁掉的关键指代如何补救？
- 需要出示：选择理由、裁剪记录、超预算案例与最终输入。
- 验收要点：先限制可读范围再挑内容；可解释裁剪，不用无限增大上下文掩盖选择问题。

### E04｜摘要错了，系统会不会一直沿用错误？【P5、P7】

- 核心问题：如何保留原始事件、标记摘要来源并处理新旧信息冲突？
- 递进追问：用户纠正论文范围后哪些状态要失效？模型推测的偏好能否直接变成用户事实？
- 需要出示：事件与摘要关系、纠正流程和错误摘要测试。
- 验收要点：摘要可被修正并追溯；区分用户明确陈述、系统推断和论文事实。

### E05｜共享 FAQ 或阅读笔记如何避免过时？【P5、P8】

- 核心问题：派生知识怎样绑定论文、版本与证据？
- 递进追问：论文新版发布、证据删除或共享权限撤销后怎么办？旧笔记还能否被检索到？
- 需要出示：来源关联、失效处理与回源核验测试。
- 验收要点：派生知识帮助导航，最终结论仍回到可读原文；失效不能只依赖不及时的缓存删除。

### E06｜用户怎样控制自己的长期记忆？【P5】

- 核心问题：什么值得保存，保存多久，怎样查看、修改和删除？
- 递进追问：删除时要处理哪些缓存、向量、摘要和使用记录？审计记录如何避免继续保存已删内容？
- 需要出示：最小记忆 Schema、生命周期与删除验证。
- 验收要点：只保留有用途的信息，明确删除边界；TTL 不是完整的数据治理方案。

### E07｜如何证明 Memory 有帮助而非增加污染？【P5、P7】

- 核心问题：有哪些必须比较“无记忆”和“有记忆”的多轮场景？
- 递进追问：准确率提高但跨论文串话增加，是否值得上线？Redis 丢失后用户会话怎样恢复？
- 需要出示：指代、切话题、歧义、长对话与会话隔离的分层结果。
- 验收要点：同时评估收益、污染率、成本和恢复行为；单轮 QASPER 得分不足以证明多轮能力。

## F. 反馈与可执行 Skill

### F01｜点赞、点踩、追问和复制各能证明什么？【P6】

- 核心问题：哪些反馈是强信号，哪些只表示用户发生了行为？
- 递进追问：追问一定说明答错吗？复制一定说明正确吗？重复反馈、恶意反馈、没有反馈怎么处理？
- 需要出示：反馈 Schema、与 Trace 的关联及信号权重规则。
- 验收要点：不把隐式反馈直接当正确标签；保留来源、去重和可复核依据。

### F02｜retrieval、intent、generation、knowledge_gap 怎么区分？【P6】

- 核心问题：一次“把基线结果当成本文结果”的错误如何定位根因？
- 递进追问：只改问题范围、只换证据、只换生成条件后分别会怎样？何时不能确定是知识缺口？
- 需要出示：原 Trace、分步核对或干预实验、归因置信度与人工复核。
- 验收要点：根因依据可检验，不让模型凭最终答案猜；“没检索到”不自动等于“原文不存在”。

### F03｜Skill、Hook、Rule 和提示词有什么区别？【P6】

- 核心问题：哪个对象表达可执行步骤，哪个响应事件，哪个约束不可违反的边界？
- 递进追问：调 top-k、扩展术语、限制跨论文引用分别放在哪？两个 Skill 冲突怎么办？
- 需要出示：有版本的策略 Schema、执行器白名单和冲突处理。
- 验收要点：命名之外有真实执行语义；明确参数上下限，策略不得提升权限或关闭强制核验。

### F04｜怎样证明反馈真的改变了下一轮执行？【P6、P7】

- 核心问题：从一次纠错到后续问题，提供完整的行为变化证据。
- 递进追问：只改回答措辞算改进吗？Skill 没触发时怎么算覆盖率？行为变了为何仍可能没有质量收益？
- 需要出示：反馈 ID、候选版本、命中原因、执行前后参数、证据与结果。
- 验收要点：分别证明“策略被执行”和“效果更好”；不以策略表新增一行代替闭环。

### F05｜候选策略怎样回放，怎样防止只记住失败题？【P6、P7】

- 核心问题：候选的修复集、回归集和保留集如何划分？
- 递进追问：同一模型既生成候选又判分怎么办？线上论文更新后历史回放应该使用哪个快照？
- 需要出示：候选/基线同题结果、评测配置、退化样本与发布门槛。
- 验收要点：修复某题不能以破坏整体表现为代价；冻结验收集不参与反复候选调优。

### F06｜灰度怎么分组，怎样知道提升可信？【P6、P7】

- 核心问题：稳定哈希的 key 选用户、会话还是请求？为什么？
- 递进追问：策略升级会不会让同一用户换组？多个实验互相干扰怎么办？没有真实流量时怎样诚实验证？
- 需要出示：分桶代码、组间样本量、配置快照与离线/在线来源说明。
- 验收要点：分组单位与干扰风险清楚；小样本、仿真和离线对照不能冒称线上显著提升。

### F07｜回滚恢复的究竟是什么？【P6、P8】

- 核心问题：停用一个 Skill 与恢复上一版完整策略配置有什么区别？
- 递进追问：同时发布、正在执行的请求、缓存与实验版本怎样保持一致？模型能否自行把候选直接投产？
- 需要出示：不可变策略版本、发布记录、回滚演练及请求使用的版本。
- 验收要点：有明确恢复目标、触发条件和发布权限；默认候选先回放再审核，不能把自动写库称为安全自进化。

## G. 存储与权限

### G01｜数据库、向量索引、对象存储和 Redis 各放什么？【P0、P2、P5】

- 核心问题：论文原件、解析正文、chunk、embedding、会话、Trace 和任务分别存在哪里？
- 递进追问：为什么不把 PDF 全塞数据库或 Redis？S3 兼容是否意味着不同服务的行为完全一样？
- 需要出示：数据归属表、主键关系和一个完整读取链路。
- 验收要点：明确事实来源、可重建派生数据与缓存；对象存储产品尚未确定时不编造部署细节。

### G02｜同一篇论文的不同版本怎么管理？【P2、P4、P8】

- 核心问题：paper_id、version_id、内容哈希和 chunk_id 分别表达什么？
- 递进追问：同名不同论文、重复上传、同一论文修订怎么区分？新版是否意味着旧版不再允许引用？
- 需要出示：版本关系、去重策略、引用快照和版本比较用例。
- 验收要点：不用标题单独判定身份；论文旧版可保留和明确选择，不能照搬“旧制度一律作废”。

### G03｜文件上传成功，但数据库写入失败怎么办？【P4、P8】

- 核心问题：上传、校验、登记、解析、索引与发布可读状态怎样衔接？
- 递进追问：数据库成功但文件缺失、任务重复、用户重试和一半文件解析失败分别怎么办？
- 需要出示：入库状态机、幂等键、错误恢复与孤立对象清理演练。
- 验收要点：不假设对象存储与数据库共享事务；明确补偿、重试及何时对用户可见。

### G04｜对象存储的链接可以直接发给用户吗？【P4、P8】

- 核心问题：私有论文怎样授权下载、预览与引用跳转？
- 递进追问：预签名链接被转发、过期、权限撤销或日志记录后有什么影响？前端隐藏按钮能防访问吗？
- 需要出示：服务端授权入口、短时访问方案、对象 key 与越权请求测试。
- 验收要点：理解链接持有者的访问能力和撤销限制；不把 bucket 公开或难猜路径当访问控制。

### G05｜空间隔离怎样贯穿整条链路？【P8；前期作为设计约束】

- 核心问题：私人论文库和共享项目空间中，身份与权限在哪些环节校验？
- 递进追问：body 里的 workspace_id 是否可信？BM25、向量、缓存、记忆、Trace、下载接口会不会各漏一处？
- 需要出示：授权关系、查询条件、跨空间访问与权限撤销测试。
- 验收要点：由服务端身份推导可读范围；不能只在最终回答过滤，底层证据与旁路资源也需隔离。

### G06｜数据库事实和检索索引不一致怎么处理？【P2、P8】

- 核心问题：新增、更新、删除论文时，怎样识别未完成索引或陈旧候选？
- 递进追问：多个 Worker 并发处理、乱序任务和重建索引时怎样避免发布旧版本？本地 BM25 在多个实例如何一致？
- 需要出示：索引 generation、发布切换、重建与回源过滤演练。
- 验收要点：知道事务覆盖范围与最终一致窗口；索引可重建、读路径可防陈旧证据。

### G07｜并发修改、迁移、备份和删除如何保证可恢复？【P8】

- 核心问题：两人同时发布策略或更新论文时怎样防止覆盖？
- 递进追问：选唯一约束、事务锁还是乐观版本？数据库回滚后对象版本怎么对齐？备份成功等于恢复成功吗？
- 需要出示：并发冲突用例、迁移回退方案、备份恢复记录与删除策略。
- 验收要点：防丢更新与重复发布；恢复经过演练，事实、对象与策略快照具有对应关系。

## H. 可靠性、性能与工程复盘

### H01｜如何让别人复现你的阶段成果？【P1、P7】

- 核心问题：从干净环境到运行与评测，需要哪些命令、版本和数据？
- 递进追问：全量结果和 smoke test 如何区分？当前跑的是否只是 fixture？代码复用了哪些来源？
- 需要出示：安装说明、依赖记录、数据哈希、命令、报告与对应提交。
- 验收要点：结果可追踪，运行范围诚实；复用代码能解释和验证，不把原项目的测试结果挪用。

### H02｜为什么需要队列，Redis Streams 怎样避免任务丢失或重复生效？【P6、P8】

- 核心问题：入库与反馈任务为什么不总在 HTTP 请求里完成？
- 递进追问：消费组、ACK、pending、重领和死信怎么配合？写数据库后 ACK 前崩溃会怎样？为什么不直接选 Kafka？
- 需要出示：任务状态、幂等处理、Worker 崩溃恢复和重试上限。
- 验收要点：承认重复交付并控制副作用；不宣称简单加队列就获得 exactly-once。

### H03｜整条模型服务不可用时系统还能做什么？【P2、P3、P8】

- 核心问题：检索、重排、改写、生成与核验分别不可用时怎样响应？
- 递进追问：模型超时后自动切供应商会改变哪些成本和语义？能否使用缓存答案，权限和版本怎么检查？
- 需要出示：故障矩阵、注入实验、降级提示与恢复结果。
- 验收要点：各失败模式清晰可测；降级成证据列表时如实标记，不能伪装成已核验生成答案。

### H04｜你说“更快、更便宜”时用了什么测量口径？【P2、P7】

- 核心问题：首响应、首 token、完整答案、P95、错误率和每题成本分别如何计算？
- 递进追问：缓存热冷、API 限额、失败重试和核验调用是否计入？只报告成功请求时延会掩盖什么？
- 需要出示：阶段耗时、模型用量、费用计算依据与测试条件。
- 验收要点：定义分母与测量窗口；同时报告质量、延迟、成功率和成本，不能只挑最好的一项。

### H05｜限流、缓存和背压应放在哪里？【P3、P8】

- 核心问题：并发增加时如何保护模型配额、连接池和 Worker？
- 递进追问：缓存 key 为什么需要权限范围和策略版本？缓存击穿、惊群、队列积压与用户取消怎样处理？
- 需要出示：限额策略、排队上限、缓存失效规则与过载演练。
- 验收要点：负载超过能力时有可解释的拒绝或排队；缓存不得跨用户泄漏或保留失效事实。

### H06｜何时需要服务拆分、HPA 或专用向量服务？【P8】

- 核心问题：数据从千篇变为十万篇，首先测什么，再决定改哪里？
- 递进追问：CPU、inflight、队列长度为何可能给出不同扩容信号？为什么副本翻倍吞吐不会必然翻倍？
- 需要出示：瓶颈剖析、容量假设和实际做过的负载实验；未做时提供验证设计。
- 验收要点：区分应用、模型配额、数据库与网络瓶颈；没有部署和压测就不写 K8s、20 Pod 等成果。

### H07｜如果面试官要求现场排一个错答，你从哪里开始？【P7】

- 核心问题：按“范围 → 数据版本 → 候选 → 证据 → 生成 → 核验 → 策略”追溯一次失败。
- 递进追问：最薄弱的模块是什么？哪项选型后来被实验推翻？再投入一周，优先修什么，怎么验证？
- 需要出示：真实 bad case、定位过程、修复提交、回归结果与仍未解决的问题。
- 验收要点：解释自己的具体贡献和学习收获；承认边界，用证据支持判断，不背诵“全链路领先”的模板。

## 与原简历六个技术点的对应

| 原技术点 | 主要问题 | 迁移到论文场景时必须讲清的变化 |
|---|---|---|
| Agent Harness 编排治理 | A07、D01–D07、H03 | 论文范围、受限工具、预算、核验和运行时选择；pi 是否使用以事实为准 |
| 可信 RAG 数据链路 | B02–B03、C06–C07、G01–G07 | 回源和权限仍需要；论文新版不自动否定旧版，事实层不强制使用 MongoDB |
| 混合检索与证据召回 | C01–C05、B05–B07 | 从制度术语转为模型、数据集、实验条件与证据段落；指标范围必须一致 |
| 分层 Memory | E01–E07 | 论文切换、指代、阅读偏好和共享笔记；不能把模型摘要当论文证据 |
| 反馈自进化 Loop | F01–F02、F05–F07 | 策略改进不是训练大模型；离线回放、模拟灰度和真实线上实验分开报告 |
| 可执行 Skill Workflow | F03–F04、D02、G07 | 查询扩展、top-k、章节偏好、模板等需改变真实路径并留下版本与执行证据 |

## 阶段学习验收记录模板

```text
阶段：
日期与代码提交：
工程验收报告：
本次抽题编号（3–5 道）：
已能解释并出示证据的内容：
仍混淆的概念：
需要补做的一个小实验：
复测安排：
学习验收状态：未进行 / 待复测 / 已通过
```

初始状态：所有阶段学习验收均未进行。工程验收结果以各阶段独立报告为准。提交 GitHub 的门槛是该阶段约定的工程检查通过并记录结果；提交代码不自动改变学习验收状态。

## P2B-1 结合本次代码的递进追问（后续教学使用）

1. 为什么先用 384 维英文 BGE 和单篇精确检索？中文问题、大论文库、不同硬件会怎样改变选择？
2. 512 token 是字符数还是 tokenizer token 数？为什么要留出特殊 token，长段落窗口重叠和最大相似度聚合有什么代价？
3. 两个检索器的分数为什么不能直接相加？RRF 中 20 个候选和常数 60 分别影响什么，怎样做预先约定的消融？
4. 模型 revision、正文版本、预处理规则与向量索引怎样绑定？哪个变化要求重建，为什么维度相同仍可能不能混用？
5. 索引构建进程中途退出、论文换版、向量命中但正文不存在时，当前代码分别如何处理？事务边界在哪里？
6. 745 题中新增命中和丢失命中分别有哪些？混合结果若差于纯向量，你会如何解释，而不是只报告最好的一组？
7. 为什么离线批量问题向量的耗时不能当作用户在线延迟？首个请求加载模型、CPU/GPU、单进程锁分别有什么影响？
8. 本阶段返回原文和哈希校验为什么仍不足以证明生成答案受到证据支持？下一步的重排与 Verifier 各负责什么？

## P2B-2 结合重排实现的递进追问（后续教学使用）

1. 双编码器和交叉编码器在输入、计算和缓存上有什么不同？为何不能直接用重排模型替代全部向量检索？
2. 为什么三组都固定 20 个候选，并给混合检索在 RRF 后截取 20？如果使用并集 40 个，实验解释会有什么变化？
3. 问题和正文共同受 512 token 限制时怎样分配预算？token_type_ids 有什么作用，长段落滑窗的最大值聚合可能怎样影响排序？
4. 当候选 Hit@20 很高而重排 Hit@5 较低时，能得出什么结论？若 gold 根本未进入候选，换更大重排模型能解决吗？
5. 重排分数为什么可能为负？对其做 sigmoid 是否就成为“答案正确概率”？为什么不能直接用它判定不可回答？
6. 推理中途论文换版会发生什么？为何重排前后都需要检查，什么时候返回 409 而不是无穷重试？
7. 模型缺失或设备内存不足时，系统如何表现？用户主动关闭重排和系统静默降级有什么区别？
8. 这次提升付出了多少本机延迟？首次加载、预计算向量、CPU/MPS、服务锁与并发请求分别如何影响统计解释？


## P2B-3 结合生成与核验的递进追问（后续教学使用）

1. 为什么 JSON mode 不能代替 Pydantic 业务校验？输出截断、空字符串、多余字段和字符串布尔值怎样处理？
2. 为什么把回答建模为 claims + evidence，而不是先生成一段自然语言再匹配引用？未经核验的前言和总结会带来什么问题？
3. 引用 ID 存在、摘录在原文中、结论受到支持分别证明什么？请结合本次英文数据集误推断案例说明。
4. 为什么同一模型独立调用两次仍可能共同犯错？更换更强或不同模型裁判要怎样衡量收益、误杀和费用？
5. 已命中 gold 段落但没有答案细节时，为什么不能保证可回答？BIBREF 和图表占位符怎样改变证据质量？
6. 生成期间论文换版为何必须拦截？核验后又重新生成摘要为什么会破坏原来的核验保证？
7. 字符预算、token 使用量、缓存费率、读超时、整体截止时间和前端取消有什么区别？哪个当前已实现，哪个尚未实现？
8. 四题中 answered、evidence_insufficient、verification_failed 各自应怎样评价？如何冻结下一轮评测，避免只修这几道题就宣称可信？


## P2B 评测与调优设计追问（后续教学使用；v3 实验已完成）

1. 候选 K、重排候选数、生成上下文 K、RRF 常数与 BM25 k1 各自是什么？为什么它们不能用一个参数控制？
2. Hit@20 很高而重排 Hit@5 较低时，怎样区分融合裁剪、重排损失和证据表示缺失？为什么只扩大 K 不足以解决所有问题？
3. 加权 RRF 与原始分数加权有什么差异？两路分数尺度、零权重独有候选、同分排序应怎么处理？
4. 为什么按论文划分调参/回归集？已经看过 745 题 dev 结果之后，能否把 dev 子集重新命名成未见测试集？
5. 平衡抽样每类 20 题的宏平均为什么不是自然分布准确率？没有证据映射的生成题与图表/争议题应怎样记分母？
6. 中文生成为什么不能直接算英文 Answer F1？离线英文评测、产品中文支持率和自动翻译分别有哪些局限？
7. 已知失败案例、人工金标、模型审阅和合成挑战例有什么不同？何时可以用 gold 证据诊断，为什么不能放进在线链路？
8. 原行政助手与新项目的 RRF、重排和 Verifier 哪些一致？原代码的启发式回退与我们明确停止发布各有什么收益和代价？


## P2B-4 结合真实实验的追问（保存题目，尚未提问）

1. 为什么候选 20 不等于给生成模型 20 段？缩到 10 应同时看哪些指标？怎样隔离 RRF 常数、候选数与上下文量的影响？
2. K60、候选 20 时，0.75:0.25 为什么可能几乎退化成偏向一路的选择？为什么同一权重下换 K 后最终前五指标可能不变？
3. 本轮网格仍选 K60/等权，怎样证明调优工作有价值？小型平衡子集与完整 745 题结果不同，应该怎样描述？
4. v2 Answer F1 上升，但不可答题 dev 发布仍是 9/20，能否宣称可信度提高？完整结论与简答格式差异怎样影响 F1？
5. 如何固定生成草稿，仅替换核验器，区分生成与核验贡献？拒答、核验失败、调用故障、结构化输出失败应如何分别记分？
6. 摘录逐字匹配为什么不等于语义支持？研究使用了数据库中的七种语言，为什么不能据此回答整个数据库的规模？
7. 40 个简单合成挑战全部通过为什么仍不够？模型审阅、工程构造标签与独立人工标注分别能证明什么？
8. 为什么按论文而非问题独立 bootstrap？区间是否包含模型随机性、标注错误和反复调参造成的偏差？
9. 935 次调用的费用怎样估算？调用前预留、失败 usage 缺失、崩溃恢复和多进程同时写预算有哪些边界？
10. 为什么新旧回答策略需要共享并发门？实验参数、输出哈希、证据版本和提示版本怎样帮助复现失败？

## P2B-5 结合诊断审阅的追问（保存题目，尚未提问）

1. T02、T06 的旧版已经回答了问题，为什么 F1 仍可能为 0？怎样分开衡量语义正确、回答格式和发布决策，而不偷偷改旧指标？
2. 为什么数据集标为可答、系统返回 evidence_insufficient，不足以证明核验误拒？论文可答、当前上下文可答、当前草稿可发布分别是什么？
3. T13 的 gold 段落在候选第 4 位却没进入上下文，为什么还不能立即归因于重排？“一名朗读者”与“一名标注者”怎样暴露标注和对象范围问题？
4. 108 处引用都能定位，为什么仍会有答非所问？已有 addresses_question 字段却漏判时，增加同义字段为什么可能没有效果？
5. 如何解释本次 3 份正确拦截、7 份漏放与 2 份拦截待定？为什么不能把故障富集样本的比例当作系统全量错误率，也不能据此比较两版核验器的因果收益？
6. T04 的审阅结论在看额外原文后发生变化，应如何保留记录？模型辅助审阅、独立人工审阅、数据集标签与合成标签有什么不同？
7. 训练/测试规模只回答训练规模，与明确说“已知训练规模，测试规模尚未找到”有什么不同？什么时候应拒答、给部分结果或澄清问题？
8. 固定草稿更换核验方法的实验应固定哪些输入？为什么历史模型响应与今天的新响应直接比较仍可能受到模型服务变化和随机性的干扰？


## P3-1 阶段流与失败边界（保存题目，尚未提问）

1. 固定问答工作流为什么也需要 Harness？显式阶段约束与只增加日志字段有什么区别？
2. 原 status 与新增 run.state/reason 分别面向什么用途？HTTP 409 为什么也需要运行记录？
3. 无检索证据、上下文超限和生成器弃答为什么不能混为同一个根因？弃答后为何仍要复查版本？
4. 如何证明非法引用不会调用语义核验、语义核验失败不会发布，以及版本变化时此前调用记录仍保留？
5. 未执行阶段为何用 not_run/null，而不是通过或零耗时？历史固定证据回放的计时为什么不是在线端到端时间？
6. completed/published 为什么不等于正确答案？状态机和故障注入测试分别保证了什么，不能保证什么？
7. 当前字符上限、两次调用上限、HTTP 超时和互斥锁分别有哪些边界？离任务级预算、持久恢复和身份权限还差什么？

## P3 执行约束与运行治理（保存题目，尚未提问）

1. 当前 Agent 采用什么编排方式？FastAPI、Pydantic、显式状态机分别解决什么问题？生成和核验用了两个提示词，为何不能称为自主多 Agent？
2. HTTP I/O 超时与任务截止时间有什么不同？后台生成仍阻塞时，为什么返回超时后还要保留并发名额？迟到结果在哪些位置被拦截？
3. 为什么要先预留调用额度、保存记录，再发模型请求？预留次数、实际派发次数、供应商 usage 和最终账单为何可能不同？usage 未知时应怎样处理？
4. 48,000 输入字符、24,000 证据字符、2,048 输出 token 分别限制什么？为什么不能把字符预算写成精确 token 或金额预算？
5. Bearer 身份认证、论文授权和前端 paper_id 各有什么作用？为什么不能接受客户端自报 user_id、allowed_tools？local_public 能否被描述为多用户隔离？
6. 运行中撤权时，只有请求入口鉴权为什么不够？发布前复查、响应前复查各能阻止什么？为什么无法收回已交付内容，也不能承诺绝对无竞态撤权？
7. 为什么论文列表要在 count、排序和分页之前过滤权限？除了正文，诊断中的全库规模与其他用户 run_id 还可能泄漏什么？
8. 语义核验通过，但最终写入失败或取消先提交，能否返回答案？怎样用原子更新条件证明并发取消不会被发布覆盖？
9. 为什么修正后要重置检查并绑定新草稿哈希？为什么最多一次、默认关闭、保持相同证据？修正次数增加能否证明回答质量改善？
10. PostgreSQL 会话锁解决什么问题？失去会话后为什么不透明重连？这个方案为什么不能声称已经支持多 worker 分布式扩容？
11. 重启后标记 interrupted 与从检查点自动续跑有什么区别？模型请求发送后进程退出，为什么重试可能重复计费？唯一 run_id 能否保证供应商 exactly-once？
12. 为什么运行历史只保存元数据？这些记录能帮助定位哪些故障、不能复现哪些完整内容？工程测试、质量指标与人工正确性判断应如何分别表达？

## P4-1 运行观察与交互（保存题目，尚未提问）

1. 为什么执行问答和读取运行状态要分开？共用 run_id 如何保证观察的是同一次任务？
2. 为什么首次状态查询可能返回 404？如何区分未受理、找不到记录、撤权和临时存储故障？
3. 为什么用上一次查询结束后再安排下一次，而不直接固定间隔发送？超时、退避和观察上限分别解决什么问题？
4. 查到 published，但浏览器还没有答案正文时，能否结束原问答请求？状态记录与答案交付有什么区别？
5. cancel_requested 与 cancelled 有什么不同？为什么停止等待答案后还需要继续查询？前端 AbortController 能否证明模型停止计费？
6. 切换身份或论文后，怎样防止旧查询串入新页面？终止请求和检查迟到响应为何都需要？
7. 阶段快照查询、SSE 状态推送与模型 token 流分别是什么？为什么未核验草稿不能直接显示成最终回答？
8. 刷新后打开未完成运行，能够恢复什么、不能恢复什么？工程替身联调为什么不等于真实回答质量验证？


## P4-2 反馈绑定与并发修改（保存题目，尚未提问）

1. run_id、回答快照指纹、反馈 revision 分别解决什么问题？为什么不能只信任客户端提交的答案？
2. 指纹什么时候生成和持久化？一次修正后应绑定初稿还是最终核验通过的回答？为什么指纹不能证明回答正确？
3. 为什么普通运行记录继续只存元数据，而提交反馈后才保存问题、答案与引用快照？有什么追溯与隐私取舍？
4. 两个页面读到同一版本后分别修改，如何防止静默覆盖？为什么“保存请求失败”不等于数据库未写入？
5. 重复提交如何避免增加版本？如果另一个页面已经改过内容，旧请求还能被当作安全重试吗？
6. 撤销论文授权后，反馈读写和迟到响应如何处理？默认共享身份、授权文件复查与数据库提交各有什么边界？
7. 为什么历史快照可以展示旧论文版本，却不能宣称对应当前正文？引用短句、段落指纹与当前上下文的作用有什么不同？
8. 用户反馈为什么不是人工正确性标签？从反馈采集到策略改进还缺哪些 P6 验证？当前是否保留完整编辑历史？


## P4-3 PDF 入库与来源定位（保存题目，尚未提问）

1. PDF 字节、提取文字、文本块与引用各有什么身份？为什么文件页序不等于印刷页码？
2. 能提取文字为什么不代表理解了双栏、表格或公式？如何发现扫描页，又有哪些漏检？
3. 为什么在独立进程解析 PDF？大小、解压内容、时间和内存限制分别防什么，macOS RSS 采样有什么边界？
4. 对象上传成功但数据库发布失败会怎样？为什么不能承诺跨 S3 和 PostgreSQL 的原子事务？
5. 同文件改名后重复上传为什么复用记录？更换解析器或文件内容为什么应改变身份？
6. PDF 上传为什么不直接触发已有 QASPER 全库索引重建？单篇 BM25 与原全库词频有什么区别？
7. 默认公共上传与私有论文授权有什么差别？下载原 PDF 为什么仍要经过服务端校验？
8. 文件入库、解析可读、检索相关、回答正确分别如何验证？合成替身联调能证明什么，不能证明什么？

## P5-1 最小工作记忆（保存题目，尚未提问）

1. 运行记录、工作记忆和原文证据分别回答什么问题？为什么保存聊天历史本身还不能构成完整的 Memory 治理？
2. 会话为什么同时绑定身份、论文版本、revision 和到期时间？论文版本与会话 revision 能否互相替代？
3. 追问只让生成器看到上文、核验器看不到，会有什么问题？为什么旧回答不能充当证据，即使上一轮曾核验通过？
4. 窗口轮数、字符上限、输出截断、固定 TTL 分别约束什么？到期不可读与数据库物理清除为何不是同一件事？
5. 有限追问规则有什么误判和漏判？“上一条只有一个 claim”为什么不等于语义上只有一个对象？
6. 清空后旧请求返回，如何避免记忆被复活？两个请求持有相同 revision，为什么只能接受一次追加？
7. 回答已经发布但记忆保存失败，应该如何展示？为什么不能自动重发模型请求？
8. 关闭记忆、切换论文、清空会话、刷新页面各有什么不同？为什么数据库持久化与页面恢复需要分别实现？sessionStorage 中的定位信息为什么不能当成授权？
9. 默认共享本机身份有什么隔离边界？论文撤权后为什么仍允许有效身份删除自己的旧会话？
10. FakeModel 多轮测试能证明哪些数据契约与执行约束，不能证明哪些实际理解能力？本次工程完成为什么不能写成回答准确率提升？

11. 为什么 claim 数量不是语义对象数量？修掉程序误拦截后，如何区分“允许进入模型判断”与“保证指代正确”？
12. 页面恢复时如何防止旧请求覆盖新身份或新论文？凭证未持久化时如何恢复；临时故障与已过期/撤权为什么采用不同处理？
