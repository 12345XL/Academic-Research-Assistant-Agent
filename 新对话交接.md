# 科研论文助手 Agent：新对话交接

> 后续进展（2026-09-23）：P3 已在本文件所指的科研项目中完成指定论文、单实例工程验收。新增显式阶段控制、任务时限与调用预算、服务端论文授权、PostgreSQL 运行记录与取消/重启中断、一次可选修正及重新核验；295 项后端、37 项前端测试与构建通过。编排仍为 Python 固定工作流，没有引入 LangGraph/pi。当前完整状态与学习顺序见 [P3 验收与学习路线](docs/P3验收与学习路线.md)；Git 提交以项目实际日志为准。以下正文保留制作交接时的历史快照，其中“尚未实施 P3”“下一步建议 P3”不再代表最新状态。人工审阅扩展仍暂停，P2B 质量问题与用户学习验收仍未完成，不自动进入 P4。

交接日期：2026-09-23。请在新对话中上传本文件，并说明“按这份交接继续”。本文件记录当前状态与最新纠偏方向；实际执行仍以用户在新对话中的请求为准。

## 1. 用户真正的目标

从 0 到 1 实现“基于 Harness Engineering 的可信 RAG 与反馈改进科研论文助手”，面向 AI 应用 / Agent 工程师岗位。用户要在做项目时理解简历上的技术点，能解释原理、选型、代码、取舍、失败案例和验证方法。

- 技术选型以系统适合什么为依据，不以用户当前熟悉什么为依据；解释为什么选它、为什么暂不选其他方案。
- 利用现有代码推进，但不能只交付用户不理解的成品。每个阶段保留必要讲解和可验证的实例。
- 不为了复现参考简历中的数字而包装指标、搭建无必要的组件或宣称未做过的部署规模。
- 面试题写入 `科研论文助手Agent面试问题.md`。用户未开始面试时不突然提问。
- 阶段交付检查通过后，已获授权 commit + push 到指定仓库，不强推，不上传密钥或语料全文。

## 2. 最新纠偏：新对话首先要知道

用户最近明确表示“越来越偏了”。原因是工作从实现和学习核心 Agent，逐渐转向持续调参、复杂诊断及人工审阅工具，并把完成 24 题人工标注变成后续开发的前置条件。

上轮已承认推进失衡，提出以下纠偏方向，作为新对话建议起点：

1. **暂停评测工具扩展。** 人工审阅页保留，不要求用户必须完成 24 题标注才能继续实现核心系统。
2. **P2B 基础工程可作为后续开发基线。** 检索、生成、引用和核验已有运行实现；生成质量存在缺陷且未验收，不能把这点改写成“质量已达标”。
3. **下一步建议回到 P3：受控 Harness。** 先核对已有能力，再补控制流程、状态、运行约束与可追踪记录，避免重复造已有的 Trace、超时或校验功能。
4. 后续沿核心流程推进交互、会话记忆和一次可回放、可撤销的反馈策略改进；最终再系统评测与整理简历。
5. 保留必要回归与失败案例，不默认继续调 RRF、扩大候选量、换模型或扩建评测平台。

**人工审阅没有完成，但它不再是核心工程继续推进的硬性前置条件。** 也不能用新一轮模型审阅冒充人工结论。

注意历史冲突：当前 `项目规划.md`、P2B-4/5/6 文档里仍有“质量未过，不进入 P3”“先完成人工复核”等旧推进安排。实验记录仍有效；这些推进安排需要在新对话根据最新纠偏同步更新，不能机械沿用旧安排。本次只制作交接文件，没有实施 P3，也没有替用户创建新任务。

## 3. 正确的项目目录

当前科研论文项目：

```text
/Users/xl/Desktop/居丽叶的简历项目9：行政助手Agent/科研论文助手Agent
```

旧行政助手参考项目：

```text
/Users/xl/Desktop/居丽叶的简历项目9：行政助手Agent/wenshu-project
```

Codex 的工作目录可能仍默认指向旧项目。读写科研助手文件、运行检查和 Git 操作时必须明确使用科研论文项目目录；不要误改旧项目。

远程仓库：<https://github.com/12345XL/Academic-Research-Assistant-Agent>

生成本文件前，科研助手工作区干净，分支为 `main`，代码基线为 `0bfc290`。最近提交：

- `0bfc290`：人工复核页面与审阅口径。
- `20ea572`：已有 24 题轨迹的诊断审阅。
- `97edbc1`：Markdown 普通预览兼容调整。
- `5d8680d`：加权 RRF 与分层生成策略对照。

这是历史快照；新对话先检查实时 Git 状态，不覆盖用户后续改动。

## 4. 当前技术栈与数据范围

| 部分 | 已采用方案与边界 |
|---|---|
| 前端 | React、TypeScript、Vite；已有论文工作台 |
| 后端 | Python、FastAPI |
| 事实与业务数据 | PostgreSQL；论文正文、版本和浏览元数据 |
| 向量检索 | pgvector、本地 BGE 编码；当前为指定论文范围检索 |
| 关键词与融合 | BM25、RRF、可配置的加权融合 |
| 重排 | 本地 MiniLM 交叉编码器 |
| 对象存储 | SeaweedFS 的 S3 接口；结构化论文正文对象，不是已经缓存了全部 PDF |
| 生成与语义核验 | DeepSeek；当前生成和核验为同模型的独立请求，不能视为独立真值 |
| 数据 | QASPER 官方 train/dev 的 1,169 篇论文、61,317 段正文、61,467 个向量窗口 |

QASPER 论文正文用于 RAG；问题、标准答案、证据标注只用于离线评测。不得进入在线索引、回答提示或 Memory。官方 test 未用于开发调参；dev 已经多次曝光，只能称开发对照。

本地 `.env` 已配置过 DeepSeek。不要在文档、日志、工具输出或回复中展示密钥，不要求用户重复粘贴；先核对配置是否可用。原来的预算和少量联调授权，不等于可以无限追加付费评测。

原行政助手的 MongoDB、Milvus、Redis、pi、部门隔离及 K8s 等是参考设计或后续候选。当前项目采用自己的技术栈，不得声称已原样实现参考简历全部能力。

## 5. 已完成什么，尚未完成什么

| 阶段 | 当前状态 |
|---|---|
| P0 | 场景、技术选择、差异说明、规划和面试题库已有 |
| P1 | 数据准备、正文与标注隔离、BM25、CLI/API 基线已验收 |
| P2A | PostgreSQL/S3 导入、论文工作台、方向筛选、方向内排序、可调分栏已验收 |
| P2B-1/2 | 向量、混合检索、重排、实测消融已有 |
| P2B-3/4 | 生成、引用硬校验、语义核验、发布前版本复查及分层实验已有；质量未达标 |
| P2B-5 | 24 题、48 份已有输出的模型辅助诊断已完成，不是独立人工金标 |
| P2B-6 | 人工审阅工具准备完成，真实人工标签仍 pending；工具扩展现建议暂停 |
| P3 | 尚未完成正式 Harness 阶段；现有链路已有部分控制和 Trace，需先盘点 |
| P4–P6 | 后续交互扩展、分层记忆、反馈与 Skill 尚未完整实现；基础问答 UI 已有 |
| P7/P8 | 最终质量验收与可选扩展未完成 |

工作台已支持编号、时间、CCF 排序，以及按方向筛选后排序。时间主要来自 arXiv 首次提交时间，不能泛称期刊发表时间。CCF 是发表载体的保守映射，大量论文等级未知，不强行补等级。PDF 是官方入口，不等于完成自选 PDF 上传、解析、存储和索引的全流程。

历史 P2B-4 验收记录为后端 193 项测试（含真实存储）、前端 20 项测试与构建通过。这是历史结果，新对话按实际改动运行必要检查，不能直接宣称当前工作已验证。

## 6. 已知实验结论：保留边界，不继续陷入调参

- 745 题指定论文检索中，向量加重排 Hit@5 为 74.09%，混合加重排为 73.29%。这是检索命中，不能写成答案正确率。
- 240 道分层问题完成 v1/v2 配对比较，dev 子集 Answer F1 从 17.59% 到 40.42%。生成格式和核验同时改变，无法把提升全部归因于核验。
- v2 在 dev 不可答题中仍发布 9/20。生成器直接 evidence_insufficient 的比例上升，不等于核验误拦上升，因为这条路径没有调用核验器。
- 40 个简单合成挑战新旧版本都通过，不能证明真实问题漏判已解决。
- RRF 平滑常数与权重实验没找到优于默认的配置；仍保留 K60、等权。用户提到的 K 指 RRF 平滑常数，与候选量 20、上下文上限 5 是不同参数。
- 240 题生成实验使用相同的向量加重排上下文，不能把其失败归因于 RRF 权重。
- 24 题诊断发现“事实有出处但未答到问题”、评分格式影响、上下文丢失以及标注范围分歧。预先模型审阅有 7 份不合格草稿被放行、3 份被拦截、2 份拦截待定；故障富集样本不能外推整体错误率。
- T13 的标准证据描述朗读者，而问题询问标注者；找到 gold 段落不必然证明答案成立。数据集标签、模型审阅、人工判断和合成标签必须分开。

这些问题进入已知限制与后续回归，不能假装解决；也不应使整个项目无限停留在 P2B。始终区分工程功能完成、评测指标变化和真实回答质量提高。

## 7. 新对话建议从哪里开始

建议先交付一个小而完整的 P3 起步增量：**拿一次已有真实问答记录讲清链路，再补齐最需要的流程控制与可观察性。** 新对话用户确认继续后执行；本文件本身不触发代码实现或付费调用。

1. 阅读当前代码，把“已实现 / 部分实现 / 未实现”列清楚，解释已有请求如何经过检索、上下文选择、生成、引用检查、语义核验和发布。
2. 优先复用已有真实调用记录演示每一步输入、输出、状态、耗时与失败原因。旧记录回放明确标为历史回放，不能冒充刚刚在线执行。
3. 对照 Harness 目标选一个有实际缺口的增量，例如显式运行状态与统一终止原因；复用已有校验和结果结构。不要先引入新框架、多 Agent 或大量服务。
4. 预算和超时要准确区分：字符上下文预算不是 token 预算；HTTP 单次读超时不是整个任务截止时间；`evaluation_budget.py` 是本地付费评测账本，不是生产请求预算服务。
5. 单篇论文范围过滤不是多租户鉴权。身份范围、权限能力、状态持久化等先说明当前边界，不因写了一个字段就宣称完整治理。
6. 用小规模、确定性的失败用例验证改动：无证据、核验失败、模型不可用、超时、预算终止、版本变化等按本增量涉及的路径选取。保证失败可解释、未经核验的草稿不发布，不要求重新跑全量模型评测。
7. 交付后讲清原理与取舍、运行证据和仍未解决的问题。更新规划与面试题，按授权提交，通过一个增量后再推进下一步。

不要把回答分数达到某个阈值当作开始 Harness 的前提。Harness 的可验证目标是让流程可控、故障可定位、失败有明确边界；它不能保证模型语义判断总是正确。

## 8. 最少必要的阅读入口

先看以下文件，不必先重读全部评测材料：

1. `AGENTS.md`、本交接文件、`项目规划.md`：协作规则、最新方向和历史阶段。
2. `docs/架构选型与数据流.md`、`docs/原项目参考与差异.md`：选型理由与参考工程的差异。
3. `src/research_agent/api.py`：请求契约和接口。
4. `src/research_agent/service.py`：检索、范围约束、事实回查、重排与检索 Trace。
5. `src/research_agent/generation.py`：上下文选择、生成/核验结构、调用与错误处理、引用检查和发布版本复查。
6. `frontend/src/App.tsx`、`frontend/src/api.ts`：已有问答 UI 与客户端契约；避免再建一套核心工作台。
7. `tests/test_generation.py`、`tests/test_api.py`、`tests/test_quality_v3.py`：相关契约与失败用例。

需要查实指标时看 `docs/P2B-4实验结果与学习路线.md` 与对应 `reports/`。需要查诊断案例时看 `docs/P2B-5轨迹审阅结果与决策.md`、`reports/p2b_trace_audit_review_v1.json`。

`.local/generation-quality-v3/` 和 `.local/trace-audit-v1/` 是本机旧调用及审阅材料，含原文，不提交 Git；新克隆不会自动具有它们。

## 9. 运行与交付注意事项

| 地址 | 用途 |
|---|---|
| `http://127.0.0.1:5173/` | 核心论文工作台 |
| `http://127.0.0.1:8011/docs` | 核心 API 文档 |
| `http://127.0.0.1:8012/` | 独立人工审阅页；不是主产品，当前不继续扩建 |

服务可能在切换对话后停止，地址只标明配置用途，不保证此时存活。先检查本机已有服务与数据，不盲目重装、重复导入或重新生成向量。

需要恢复时，在科研论文项目根目录按现有状态使用：

```bash
.venv/bin/python scripts/local_services.py status
.venv/bin/python scripts/local_services.py up
.venv/bin/python scripts/run_api.py
```

前端在另一个终端运行：

```bash
npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173
```

按改动选择验证。现有入口包括后端 pytest、前端测试/构建、`scripts/check_markdown.py`。不要把会触发付费模型的评测脚本当作普通测试随手执行。

此仓库 Markdown 要兼容普通预览器：技术公式用 `text` 代码块，金额用 USD，不用正文数学定界符。交付前检查文档格式和 Git 差异，不提交 `.env`、语料、个人 PDF、大型模型或本机运行目录。

## 10. 可以直接粘贴到新对话的开场

> 请读取附件《新对话交接.md》，从科研论文助手 Agent 的现有代码继续。我的主要目标是完成核心 Agent，并在实现过程中理解简历技术点。上一段对话过度投入评测与人工审阅工具，现在暂停这条扩展，不把完成 24 题人工标注当作继续开发的前提。
>
> 请先核对正确项目目录、Git 状态和已有问答链路，说明哪些 Harness 能力已经有、哪些有实际缺口，再选择一个小而完整的 P3 增量带我实现。复用现有代码、工作台和调用记录；不要直接重新调参、重跑全量评测或新增一套系统。保留质量问题与必要回归，但不把工程进展、指标改善和真实回答质量混为一谈。先讲清楚当前链路和本次增量，再动手。
